export const metadata = {
  title: 'Generator pachet electric',
  description:
    'Descrie-ti proiectul si primesti o oferta personalizata cu toate materialele electrice necesare — gratuit.',
};

export default function GeneratorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
